 pipeline{
 agent any
	// agent {docker { image 'node:13.8'}}
stages{
	stage('Build'){
		steps {
			echo "hi"
			echo "node version is"
			//sh 'node --version'
			echo "this is declarative pipiline"
			}
	}
}
}
